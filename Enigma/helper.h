#ifndef HELPER_H
#define HELPER_H

#include <iostream>
#include <vector>
#include <algorithm>
#include "constants.h"

using namespace std;

//Function to check if a number is repeated
bool is_repeated(vector<int> vector, int number);
//Function that converts character int integer
int char_to_int(char ch);
//Function that converts character int integer
char int_to_char(int ch);

#endif
